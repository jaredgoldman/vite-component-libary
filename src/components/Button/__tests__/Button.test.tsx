import { render, screen } from '@testing-library/react'
import Button from '..'

describe('Test', () => {
  it('Should be able to test a simple calculation', () => {
    expect(1 + 1).toBe(2)
  })
})
